# Python
.venv/
.env
.env.*
!.env.example
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Cursor / IDE
.cursor/
.cursorignore
*.code-workspace

# OneDrive / Windows junk
desktop.ini
Thumbs.db
~$*

# Uploads from the Flask UI
web/uploads/

# Huge source drawing set (keep locally to retrain)
data/Изометрии.pdf

# PDF copies inside output jobs (sources live in data/)
output/**/*.pdf

# Keep marked isometries in git:
#   output/Изометрии_без_API/     — разметка 1–277
#   output/тест_1_без_API/ …      — контрольные тесты
#   output/02_Изометрии_10_листов_без_API/
#   output/проверка_мастеру/
#   output/tests_summary.json

# Keep models/iso_clf.pkl in git (≈0.5 MB, LFS not needed).
# ��������� �������, �� � ����
_audit_train.py
models/iso_clf.pkl.before_*
models/label_audit.*
models/dataset.csv
models/dataset.jsonl
output/_audit_crops/
output/_clip_*
output/_gold_*
output/_sheet*_hires.png
output/_top_left_hires.png
