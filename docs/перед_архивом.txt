# Что не класть в git / zip

- `.venv/`
- `.cursor/`
- `.env` (ключа и так нет, на всякий случай)
- `web/uploads/`
- `__pycache__/`
- `data/Изометрии.pdf`
- `_audit_train.py`, `models/iso_clf.pkl.before_*`, кропы в `output/_...`

Оставить: код, `requirements.txt`, README, модель, десятку из задания, три контрольных pdf, размеченный output.
